{
  "version": 2,
  "framework": "vite",
  "buildCommand": "BASE_PATH=/ PORT=4173 pnpm --filter @workspace/edutoppers run build",
  "outputDirectory": "artifacts/edutoppers/dist",
  "rewrites": [
    {
      "source": "/((?!api(?:/|$)).*)",
      "destination": "/index.html"
    }
  ]
}